# Claude Code runtime data (don't version control)
todos/
shell-snapshots/
statsig/
projects/
config/

# API keys and secrets - NEVER commit these
.env
*.env
api-keys*
*secret*
*credentials*

# OS
.DS_Store
Thumbs.db

# peon-ping sound packs (large binary files - install separately)
hooks/peon-ping/packs/

# Plugin node_modules and cache (install via Claude Code plugin system)
plugins/marketplaces/
plugins/cache/

# Never commit API keys
plugins/**/.env
plugins/**/api-keys*

# tmux plugin manager (installed at runtime)
tmux/plugins/
